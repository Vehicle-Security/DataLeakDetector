"""Test package for the rewritten DataLeakDetector contract.

The suite validates the canonical package under main/data_leak_detector rather
than old stage directories, so regressions are caught against the new
single-package architecture.
"""
