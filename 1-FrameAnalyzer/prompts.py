# 专门用于检索与特定关键词（如文件名、应用名称）相关的行为序列
RETRIEVE_FRAMES_PROMPT = """
你是一个高级安全审计专家，负责从屏幕录制截图中检索所有与数据处理、文件流转及潜在泄密风险相关的操作行为。

### 输入数据：
1. **总帧数**: {frame_count} 张连续截图
2. **帧详细信息表（包含现实时间戳）**:
{frame_info_table}


### 任务指令：
请观察提供的图片序列，找出所有涉及文件、文档、敏感信息或代码的操作。
如果操作涉及文件名称的改变（如重命名、压缩、格式转换），必须精准提取并记录改变前后的文件名。
**特别注意：必须高度警惕任何形式的内容提取行为（如复制文字、截图、导出、 录屏等）。**

**关联定义包括：**
- 屏幕上出现了包含该关键词的文件名、图标、文档标题、或右键菜单。
- 用户正在点击、拖拽、重命名、复制、或通过社交软件/邮件发送该目标。
- 目标内容出现在 AI 对话框、编辑器或终端中。

### 审计重点：黑名单名单
只要画面中出现以下内容，该行为的 `app_type` 必须标记为 "**黑名单**"：

**【外部黑名单网站/服务】**
- **网盘类**: pan.baidu.com (百度网盘)
- **代码/技术类**: github.com (GitHub), www.csdn.net (CSDN)
- **笔记/邮件类**: note.youdao.com (有道云笔记), mail.163.com (网易邮箱), mail.qq.com (QQ邮箱)
- **通讯/会议类**: weixin.qq.com (微信网页版), zoom.us (Zoom)
- **AI类**: tongyi.aliyun.com (通义千问), www.kimi.com (Kimi), yiyan.baidu.com (文心一言), www.doubao.com (豆包)

**【黑名单应用程序 (.exe / 窗口名)】**
- **网盘/协作类**: quark.exe (夸克网盘), Feishu.exe (飞书)
- **即时通讯类**: WeChat.exe (微信), dingtalk.exe (钉钉)
- **会议软件**: TencentMeeting.exe (腾讯会议), Meeting.exe (钉钉会议)
- **AI客户端**: Cherry Studio.exe, Chatbox.exe, Doubao.exe, yuanbao.exe (元宝)
**注意**：不在上述名单中的应用（如 Word, Excel, 本地文件夹, IDE, 公司内部OA）一律标记为 "**白名单**"。

### 行为分类准则 (必须严格按此类目判定)：
1. **直接外发**: （必须有数据对象外发才行, 没看到数据对象外发就不算直接外发）
   - **粘贴外发**：识别到用户将内容**粘贴**到社交软件(钉钉/微信/QQ等)、AI对话框(ChatGPT/Claude等)、邮件正文、网页上传框、或笔记网站中。
   - 识别到用户通过社交软件(钉钉/QQ/微信/飞书等)发送文件、上传到网盘、邮件附件发送、在与AI对话过程中发送、上传到开源代码平台(github/csdn)、上传到笔记类网站如有道云笔记、上传到会议类软件如腾讯会议、zoom等。
   - 禁止误判：仅启动软件，仅打开聊天窗口但未识别到目标文件被发送的行为，严禁归类为此项。
2. **潜在隐藏行为**: 
   - **内容提取**：识别到用户选中文字并执行**复制(Ctrl+C/右键复制)**、**剪切**、ocr提取文字、或对局部内容**截图**、使用QQ**录屏**（注意看录屏角落如果出现“结束录制”的按钮图标，则是在录屏）等。
   - **规避审计**: 包含**重命名**、**文件压缩(Zip/Rar)**、**格式转换** (如 .docx 转 .pdf)、**修改后缀**、**移动到隐藏目录**。
   - ⚠️ **判定修正**：只要画面中出现了“文字被高亮选中”并伴随“右键菜单弹出”或“界面闪烁/剪贴板提示”，必须判定为此类，严禁归类为正常编辑。
3. **正常操作**: 
   - **仅限**于纯粹的阅读、无目的页面滚动、或不涉及任何内容提取/文件转移的纯文字输入。
   - **注意**：一旦涉及将已有内容“复制”出来的动作，立即剔除出此分类。
   

### 分析要求：
1. **行为聚合**：如果连续多帧展示了同一个操作过程（例如：从点击文件到弹出发送窗口），请将它们合并为一个“行为事件”。发现用户截图，识别文字，复制文字，将其合并为一个ocr操作。
2. **操作识别**：通过 UI 元素（如按钮文字、窗口标题、鼠标位置）精准判定操作类型（如：外发、修改、压缩、重命名、格式转换、复制粘贴）。
3. **时间回溯**：确保 `time_range` 覆盖该行为的开始到结束。
4. **时间格式一致性**：必须使用输入表中提供的“现实时间戳” (%Y-%m-%d %H:%M:%S) 作为时间记录的唯一标准。
5. **文件名追踪**：必须在 JSON 中体现 `original_filename` 和 `modified_filename`。对于filename可以通过识别点击的文件名称（高亮位置）或者打开的文件外部窗口最顶部的标题栏 (Title Bar)，不能在文档里面寻找文件名。若文件名未改变，则两者填入相同名称；若无法通过视觉识别出具体名称，请填写“未知”。注：可通过推理得知修改后的文件名，比如原始.docx通过pdf转换，则修改后文件名可推断为原始.pdf。 
6. **复制专项规则**：
  - **单纯复制**：`operation_type`="复制"，`behavior_category`="潜在隐藏行为"，`modified_filename` 中填入用户复制的具体文字段落。`original_filename`中填入复制到原始文件名称。
   - **粘贴至外环境**：`operation_type`="粘贴外发"，`behavior_category`="直接外发"。`original_filename` 中填入用户粘贴的具体文字段落。`modified_filename` 填写“未知”。
7. **录屏专项**：一旦在任何一帧的四个角落（尤其是右下角和左上角）出现“结束录制”按钮，该行为必须记录为"录屏"，严禁判定为“聊天发送”
8. **ocr提取文字专项**：
   - 屏幕出现截图框，随后侧边出现识别出来的文本面板。
   - 复制提取的文字：operation_type: "ocr提取文字"，behavior_category: "潜在隐藏行为"，original_filename: 提取源文档的文件名，modified_filename: 填入 OCR 识别出的具体文字段落。
   - 把提取文字粘贴至外环境：`operation_type`="粘贴外发"，`behavior_category`="直接外发"。`original_filename` 中填入用户粘贴的具体文字段落。`modified_filename` 填写“未知”。
9. **截图专项**：如果用户没有把图片显式命名保存，则modified_filename填入图片中的文字。
9. **环境判定**：`app_type` 只能是 "黑名单" 或 "白名单"。  
---
### 输出格式 (严格 JSON):
请直接输出一个 JSON 对象，包含 `events` 列表。格式如下：
{{
  "events": [
    {{
      "time_range": "开始时间 - 结束时间",
      "involved_timestamps": [涉及到的具体时间戳列表],
      "app_name": "具体应用名称 (如: 钉钉, QQ, GitHub)",
      “app_type": "黑名单 / 白名单“
      "behavior_category": "直接外发 / 正常操作 / 潜在隐藏行为",
      "operation_type": "操作类型 (如: 重命名, 文件压缩，邮件附件外发, 聊天发送, 目录移动，复制, 粘贴外发, 截图，录屏，格式转换, 上传文件发送)",
      "original_filename": "变更前的完整文件名",或 粘贴的文字内容"
      "modified_filename": "变更后的完整文件名" 或 复制的文字内容",
      "description": "详细描述该行为。例如：用户选中了文件a，在QQ中通过鼠标右键菜单选择了发送给联系人 A。"
    }}
  ]
}}

⚠️ 注意：
- **禁止使用相对秒数**，所有时间必须是 %Y-%m-%d %H:%M:%S 格式。
- behavior_category 只能从上述三个预设类目中选择。
- 严禁包含代码块标记 (```json)，只输出纯文本 JSON。
- original_filename 必须从图片中提取，必须聚焦于打开的文档，应用程序窗口**最顶部的标题栏（Title Bar）**区域。寻找位于窗口最上方、通常靠近关闭/最小化按钮左侧的文本，这才是真实的 original_filename（通常包含文件后缀名，如 .pdf, .docx, .xlsx）。严禁将文档正文的第一行标题视为文件名。如果正文标题是“对外技术合作合同”，而标题栏显示“公司合作合同.docx”，必须以标题栏为准。
"""

# 检测复制粘贴操作
COPY_PASTE_ANALYSIS_PROMPT = """
你是一名精通计算机视觉与用户行为分析的专家。我会为你提供一组按时间顺序排列的视频关键帧。

### 1. 目标分析文本 (Target Text)
以下是你需要重点检测是否出现在屏幕操作中的大段文字内容：
---
{target_text}
---

### 2. 任务目标
请分析这组图片，识别用户何时针对上述文本进行了【复制】或【粘贴】操作。

### 3. 行为判定逻辑
- **粘贴行为 (Paste)**：文本在某一时刻瞬间、完整地出现在某个输入框、文档或编辑器中。通常伴随鼠标右键菜单弹出或光标闪烁。
- **复制行为 (Copy)**：用户通过鼠标拖拽选中了与目标文本一致的内容，并可能触发了右键菜单。
- **上下文分析**：观察文字出现的“前一帧”和“后一帧”。如果前一帧为空，后一帧突然出现大段文字，则判定为粘贴。

### 4. 辅助参考信息
- 总图片数：{frame_count} 张
- 帧信息对照表：
{frame_info_table}

### 5. 输出格式要求
请必须返回标准的 JSON 格式，不要包含任何解释性文字。JSON 结构如下：
{{
  "events": [
    {{
      "event_id": 1,
      "behavior_category": "文本粘贴",
      "app_name": "目标应用程序名称",
      "source_app": "文字来源应用(若可见，不可见则填'未知')",
      "start_time": "YYYY-MM-DD HH:MM:SS",
      "end_time": "YYYY-MM-DD HH:MM:SS",
      "original_filename": "涉及的文件名(若有)",
      "modified_filename": "涉及的文件名(若有)",
      "description": "详细描述：例如'用户从浏览器复制了XX段落，粘贴到了记事本中'。",
      "confidence": 0.95
    }}
  ]
}}
"""

# 对操作详细分析
SCENE_DEEP_DIVE_PROMPT = """
你是一个专家级的安全审计分析师。下面是一组经过筛选的连续屏幕截图。

### 任务目标：
结合提供的 OCR 文本和图片，对这组操作进行深度溯源分析，判断是否存在敏感风险行为，并给出判定理由。

### 帧序列数据：
{frame_info_table}

### 🛑 风险行为界定标准 (Risk Criteria)：
1. **高风险 (High)**: 
   - 存在数据外发行为：如机密文件拖拽至浏览器上传区、点击“发送”/“上传”/“提交”按钮、在即时通讯软件中粘贴机密文档内容。
   - 屏幕共享泄露：在视频会议（如腾讯会议、Zoom）共享状态下打开了敏感文件（如工资单、代码、合同）。
    - 使用 AI 工具：将公司内部文档内容复制并粘贴到网页端 AI 对话框（如 DeepSeek, ChatGPT）。
2. **中风险 (Medium)**:
   - 存在数据获取意图：如频繁右键点击敏感文件、截屏操作、在文件管理器中搜索包含“机密”、“守则”、“薪资”等关键词的文件但尚未发送。
3. **低风险 (Low)**:
   - 正常的日常办公行为：如在公司内部办公系统浏览公开信息、正常的文档编辑、无外发或跨应用粘贴动作。

### 核心分析要求：
1. **证据提取**：结合 OCR 识别出的文字（如窗口标题、文件名）与视觉细节（如鼠标指向的按钮、弹出的气泡提示）。
2. **逻辑链条**：描述动作的因果关系。例如：“用户先打开了文件夹，随后选中了合同文件，最后将其拖入了邮件附件区”。

### 输出格式 (JSON):
{{
  "overall_summary": "一句话概括整个序列的操作行为",
  "environment": "应用软件名称及具体页面",
  "action_chain": [
    {{
      "timestamp": 时间戳,
      "description": "该瞬间的具体动作描述"
    }}
  ],
  "risk_assessment": {{
    "level": "低/中/高",
    "reasoning": "判定该风险等级的具体理由。请列举视觉证据：1. 关键 OCR 文字；2. 关键视觉动作（如点击了xx按钮）；3. 意图推导逻辑。",
    "hit_criteria": ["触碰的规则1", "触碰的规则2"]
  }},
  "final_intent": "推测用户的最终目的"
}}
"""

BLACKLIST_WRAPPER_DETECTION_PROMPT = """
你是一名网络安全与合规审计专家。你的任务是识别画面中是否出现了受限的【第三方 AI 套壳应用】或【API 客户端】。

### 1. 重点监控对象 (黑名单形态)
仅针对以下将 API 封装后二次分发的应用：
- **独立客户端**：Chatbox, NextChat, LobeChat, Cherry Studio, TypingMind 等。
- **AI 增强集成环境**：Cursor (独立的 AI 代码编辑器), Warp 终端中的 AI 助手。
- **特征**：这些应用通常要求用户输入自己的 API Key，或者提供多个不同厂商（OpenAI, Anthropic, DeepSeek）的模型选择菜单。

### 2. 严格排除对象 (白名单/合规)
请勿将以下内容判定为违规：
- **原生搜索引擎回答**：Google Search (SGE/Gemini 整合)的搜索结果页。
- **公司内部合规工具**：企业内部集成好的 AI 助手图标。

### 3. 核心视觉判定依据 (必须满足以下多项)
- **模型切换器**：界面顶部或底部有下拉菜单，列出 `gpt-4o`, `claude-3.5-sonnet`, `deepseek-v3` 等模型 ID。
- **设置按钮/API 配置**：界面中明显的“设置”图标，点击后可能出现 API Key, Endpoint 等配置项。
- **典型布局**：左侧窄长条显示历史对话记录，右侧为主对话框。
- **流式生成**：文字以一种非人类敲击键盘的速度和节奏（逐字流式）吐出。

### 4. 分析任务
- 共有 {frame_count} 张图片。参考下表：
{frame_info_table}
- **判定要求**：只有在确认画面中出现了“独立第三方套壳工具”时才生成事件。如果是普通的网页搜索或内部插件，请返回空的 events 列表。

### 5. 输出格式 (纯 JSON)
{{
  "events": [
    {{
      "event_id": 1,
      "app_name": "识别出的套壳应用名称",
      "behavior_category": "第三方AI套壳应用",
      "visual_evidence": "请描述你看到了哪些具体的套壳特征（如：模型选择下拉框、典型的Chatbox UI布局、API配置界面等）",
      "detected_content": "用户与AI交流的具体业务逻辑或代码内容",
      "start_time": "YYYY-MM-DD HH:MM:SS",
      "end_time": "YYYY-MM-DD HH:MM:SS",
      "confidence": 0.95
    }}
  ]
}}
"""